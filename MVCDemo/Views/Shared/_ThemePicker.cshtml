@*
    A 3-state theme picker (Light / Dark / Auto) modeled on the Bootstrap docs
    color-mode picker. Persists the choice to localStorage and respects the
    user's OS preference when set to "auto".

    Pure markup — all theme behavior is owned by ~/js/theme-picker.js, which is
    referenced from <head> in _Layout.cshtml so the saved theme is applied
    before the body paints (no FOUC).

    Render with:
        <partial name="_ThemePicker" model="new MVCDemo.Models.ThemePickerModel { Class = "me-2" }" />
    or simply:
        <partial name="_ThemePicker" />
*@

@model MVCDemo.Models.ThemePickerModel?
@{
    string cls = Model?.Class ?? string.Empty;
    bool showLabel = Model?.ShowLabel ?? true;
}

<div class="dropdown app-theme-picker @cls">
    <button class="btn btn-link p-1 d-flex align-items-center gap-2 app-theme-picker-toggle"
            type="button"
            data-bs-toggle="dropdown"
            aria-expanded="false"
            aria-label="Toggle theme">
        <i class="bi bi-circle-half fs-5" data-theme-icon></i>
        @if (showLabel)
        {
            <span class="d-none d-lg-inline" data-theme-label>Auto</span>
        }
    </button>
    <ul class="dropdown-menu dropdown-menu-end shadow-sm">
        <li>
            <button type="button" class="dropdown-item d-flex align-items-center gap-2" data-bs-theme-value="light">
                <i class="bi bi-sun-fill"></i> Light
                <i class="bi bi-check-lg ms-auto opacity-0" data-theme-active></i>
            </button>
        </li>
        <li>
            <button type="button" class="dropdown-item d-flex align-items-center gap-2" data-bs-theme-value="dark">
                <i class="bi bi-moon-stars-fill"></i> Dark
                <i class="bi bi-check-lg ms-auto opacity-0" data-theme-active></i>
            </button>
        </li>
        <li>
            <button type="button" class="dropdown-item d-flex align-items-center gap-2" data-bs-theme-value="auto">
                <i class="bi bi-circle-half"></i> Auto
                <i class="bi bi-check-lg ms-auto" data-theme-active></i>
            </button>
        </li>
    </ul>
</div>
